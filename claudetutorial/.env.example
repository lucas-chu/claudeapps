# The only credential the tutorial needs.
ANTHROPIC_API_KEY=sk-ant-...

# Optional, for 03_managed_agent.py: mount a real repo instead of a text
# description. See the commented-out `resources` block in that script.
# GITHUB_TOKEN=
# TUTORIAL_REPO_URL=
