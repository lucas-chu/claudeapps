ANTHROPIC_API_KEY=sk-ant-...

# Optional. Both have working defaults, so leave them out unless you need them.
#
# CANVAS_BASE_URL is deliberately not ANTHROPIC_BASE_URL: an inherited shell
# variable must never silently redirect requests, so the override is explicit.
# CANVAS_BASE_URL=https://api.anthropic.com
# CANVAS_PORT=8787
